使用使用全新开发
全新开发文档
项目名称：AetherShift（暂定，可改）
版本：v0.1 Draft
日期：2026-09-10
目标平台：Omarchy 4.x（Quattro）优先，兼容通用 Hyprland
1. 项目定位
AetherShift 是一个纯 Rust 编写的紧凑桌面范式切换守护进程。
它帮助从 Windows / macOS 迁移到 Omarchy / Hyprland 的用户，快速获得接近原系统的肌肉记忆体验。核心特点是：
* 全内存热切换（切换几乎无感知）
* 直接通过 Unix Domain Socket 与 Compositor 交互
* 单静态二进制，依赖极少
* 默认不写系统配置文件，运行时覆盖
* 可完全卸载并恢复原始状态
它不是又一个配置文件生成器，也不是 Quickshell 插件集合，而是一个独立、高性能的运行时引擎。
2. 设计原则
1. 体验优先：切换延迟目标 < 5ms，用户几乎感觉不到模式变化。
2. 最小侵入：默认只做运行时覆盖，不修改用户持久配置。
3. 能力导向：在合理安全边界内，尽可能实现真正有用的功能。
4. 单一职责：一个常驻守护进程搞定核心逻辑，CLI 只是客户端。
5. 可观测与可回滚：任何切换失败都能自动回滚，状态清晰可见。
6. 可扩展：内置高质量预设，同时支持用户自定义和动态加载。
3. 系统架构
```
┌──────────────────────────────────────────────────────┐
│                     用户交互层                        │
│  CLI  │  快捷键  │  未来 TUI / 状态指示器  │  可选 UI  │
└───────────────────────┬──────────────────────────────┘
                        │ Unix Domain Socket
                        ▼
┌──────────────────────────────────────────────────────┐
│              aethershift-daemon（Rust）                │
│                                                      │
│  ┌──────────────┐  ┌──────────────┐  ┌─────────────┐ │
│  │ Profile 引擎 │  │ 绑定虚拟层   │  │ 窗口调度器  │ │
│  │ （全内存）   │  │ （运行时覆盖）│  │ （几何计算）│ │
│  └──────────────┘  └──────────────┘  └─────────────┘ │
│                                                      │
│  ┌──────────────┐  ┌──────────────┐                  │
│  │ 事件监听器   │  │ 状态机 & 日志│                  │
│  └──────────────┘  └──────────────┘                  │
└───────────────────────┬──────────────────────────────┘
                        │ Hyprland Socket / hyprctl
                        ▼
                 Hyprland Compositor
```
关键设计点：
* 守护进程启动时完整镜像当前 Hyprland 绑定与相关规则到内存。
* 所有 Profile 切换只修改内存中的虚拟层，然后批量下发变更。
* 支持「完全接管」和「增量覆盖」两种模式。
4. 技术栈
```
| 组件    | 技术                  | 说明        |
| ----- | ------------------- | --------- |
| 语言    | Rust 2024 Edition   | 全项目纯 Rust |
| 异步运行时 | tokio               | 事件循环与并发   |
| 序列化   | serde + serde_json  | 协议与配置     |
| CLI   | clap                | 命令行解析     |
| 日志    | tracing             | 结构化日志     |
| 错误处理  | thiserror + anyhow  |           |
| 系统交互  | nix / std::os::unix | UDS、权限、进程 |
| 构建目标  | 静态链接（musl 优先）       | 单二进制分发    |
```
不引入 Python、Node、QML、Lua 作为运行时依赖。
5. 核心功能
5.1 Profile 系统
* 内置预设：windows、macos、hybrid、native
* 支持用户自定义 Profile（文件或运行时创建）
* 每个 Profile 包含：
  * 快捷键绑定映射
  * 新窗口默认策略（tiled / floating / follow）
  * 工作区行为
  * 常用系统动作（截图、剪贴板、锁屏、文件管理器等）
5.2 全内存热切换
* 切换时不写磁盘、不触发完整配置 reload（除非必要）
* 支持一键循环切换
* 切换失败自动回滚
* 可附带轻量状态反馈
5.3 绑定虚拟层
* 维护「原始状态」与「当前覆盖状态」
* 冲突检测与优先级处理
* 支持临时覆盖与永久应用（用户显式选择）
5.4 窗口与工作区增强
* 精准半屏、三分、最大化、还原
* 多显示器友好的几何计算
* 新窗口策略控制
5.5 可观测性
* 实时状态查询
* 切换历史
* 可选使用统计
* 清晰错误报告
6. 通信协议
Socket 路径：/run/user/$UID/aethershift.sock
使用长度前缀 + JSON 的请求-响应协议。
主要接口：
* status：当前 Profile、运行状态、版本
* list_profiles
* switch <name> / cycle
* get_bindings
* reload_presets（从文件重新加载，不中断服务）
* restore：清除所有覆盖，回到原始状态
* shutdown
Socket 权限严格限制为当前用户。
7. 推荐项目结构
```
aethershift/
├── Cargo.toml
├── crates/
│   ├── aethershift-daemon/      # 主守护进程
│   ├── aethershift-cli/         # 命令行客户端
│   ├── aethershift-core/        # Profile、状态机、绑定逻辑
│   ├── aethershift-hyprland/    # Hyprland 交互层
│   └── aethershift-protocol/    # 通信协议定义
├── presets/
│   ├── windows.toml
│   ├── macos.toml
│   ├── hybrid.toml
│   └── native.toml
├── docs/
│   ├── architecture.md
│   ├── protocol.md
│   ├── presets.md
│   └── development.md
├── tests/
└── scripts/
    ├── build-static.sh
    └── install.sh
```
8. 开发阶段
Phase 1：最小可用核心
* 单实例守护进程 + UDS 服务
* 内存 Profile 加载与状态机
* 与 Hyprland 事件 Socket 对接
* 实现真正的全内存热切换（核心动作：关闭、吸附、工作区、最大化等）
* 基础 CLI（status / switch / restore）
Phase 2：完整预设与体验
* 高质量 Windows 与 macOS 预设
* 冲突检测与智能合并
* 新窗口策略
* 切换反馈与完善状态查询
* 一键干净恢复
Phase 3：增强能力
* 更丰富的窗口布局
* 运行时自定义 Profile
* 使用统计与基础推荐
* 多显示器完善支持
Phase 4：可选扩展
* 轻量 TUI 或状态指示器
* 与 Omarchy Quickshell 的可选联动（非必须）
* 系统托盘支持
9. 明确不做什么（保持克制）
* 不默认修改 /usr/share/omarchy/ 或系统只读路径
* 不引入系统级输入重映射工具作为核心依赖
* 不把应用内部快捷键全局劫持作为默认功能
* 不强制用户使用特定 UI 框架
这些可以在未来作为可选高级功能探索，但不进入核心路径。
10. 成功标准
* Profile 切换延迟极低，用户主观无感
* 单静态二进制，安装与卸载简单
* 崩溃或异常后可干净恢复原始状态
* Windows / macOS 用户迁移后，主要肌肉记忆可用
* 代码结构清晰，后期扩展成本低
* 与 Omarchy 4.x 良好共存，不破坏官方体验
11. 与 Omarchy 的关系
* 优先支持 Omarchy 4.x（利用其 Hyprland + 事件接口）
* 不依赖 Quickshell 插件系统作为核心运行方式
* 可选提供与 Omarchy 生态的友好集成（例如状态同步），但守护进程本身保持独立
这份文档是从零开始、以「纯 Rust 守护进程 + 全内存热切换」为第一性原理重新设计的，不再继承之前项目的设计包袱。
需要我下一步输出：
1. 更详细的协议定义
2. 核心数据结构（Rust）
3. Phase 1 的具体任务拆解与验收标准
4. 预设文件格式示例
直接告诉我即可。